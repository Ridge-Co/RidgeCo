# GH Broker end-to-end test

Temporary file created by the GH Broker connector's `commit_file` tool to verify
write access works end-to-end (Claude -> Worker -> GitHub App installation token -> commit).

This file and its branch/PR will be deleted immediately after the test confirms.
