# Cloudflare Worker deploy config for the RidgeCo Maintenance Hub.
# Purpose: enable push-to-main auto-deploy via Cloudflare Workers Builds.
# keep_vars = true is CRITICAL — it preserves the environment variables and
# secrets set in the Cloudflare dashboard (GOOGLE_SA_KEY, QB_*, TWILIO_*,
# WORKER_SECRET, SHEET_ID, etc.) so a deploy never wipes them.

name = "maintenance-hub"
main = "worker.js"
compatibility_date = "2026-06-08"
keep_vars = true
workers_dev = true

# Daily digest cron (Session 5). 11:00 UTC = 7:00 AM Eastern during EDT (summer);
# it lands at 6:00 AM during EST (winter) — refine with DST logic later if needed.
# The scheduled() handler builds the digest but sends nothing until Config
# digest_enabled=TRUE, so this trigger is harmless until Brett turns delivery on.
# Optimizer Reviewer (B-129) now runs TWICE weekly — Mon + Wed 12:00 UTC (8am ET) —
# per greenlit item ID-1 "Increase Weekly Review Run Frequency": the mid-week run halves
# max issue-detection lag from 7 days to ~3.5. The scheduled() handler branches on
# event.cron and runs runWeeklyReview for BOTH the Monday and Wednesday crons; each reads
# Ops_Telemetry, ranks a proposal, logs to Ops_Review_Log, and delivers only if digest_enabled=TRUE.
# Weekly Open Item Report (Aug 18 session) — Monday 13:00 UTC (9am EDT/8am EST), an hour after
# the daily digest slot. Dormant until Config ar_report_enabled=TRUE AND AR_Report_OptIn has
# rows — see runWeeklyArReport in worker.js.
[triggers]
crons = ["0 11 * * *", "0 12 * * 1", "0 12 * * 3", "0 13 * * 1"]

# Git auto-deploy connected via Cloudflare Workers Builds (July 19, 2026).

# ── Environment variables & secrets (managed in the Cloudflare dashboard) ──
# These are intentionally NOT given values here: this file is committed to the
# repo, so putting secret values in it would leak them. keep_vars = true above
# preserves whatever is set in the dashboard on every deploy, so they stay in sync.
# Expected bindings (set/verify in Workers & Pages → maintenance-hub → Settings →
# Variables and Secrets):
#   Secrets: WORKER_SECRET, WORKER_SECRET_2 (rotation, temporary), GOOGLE_SA_KEY,
#            GOOGLE_SA_EMAIL, TWILIO_SID, TWILIO_AUTH, ANTHROPIC_API_KEY,
#            GOOGLE_MAPS_KEY, KEY_REGISTRY_SHEET_ID, MAKE_WEBHOOK_URL, QB_* (QuickBooks)
#   Vars:    SHEET_ID, DRIVE_PROPERTIES_ROOT
#
# Staging deploy gate (Sept 2026): this same worker.js also deploys to a SEPARATE
# Worker service, `maintenance-hub-staging` (Git-connected to the `staging` branch,
# its own independent dashboard vars — see CREDENTIALS_MAP.md "Staging sandbox").
# On that service ONLY, add a Var `STAGING` = `1` to make isStaging() explicit
# (it also self-detects off the staging-* hostname with zero setup). Never add
# `STAGING=1` to THIS service's (maintenance-hub / prod) dashboard.
# Rotation note (2026-07-20): WORKER_SECRET_2 is the new value during the
# zero-downtime swap. Once clients are confirmed on it, set WORKER_SECRET = new
# value, delete WORKER_SECRET_2, and remove the dual-accept branch in worker.js.

# 2026-07-21 EMERGENCY REDEPLOY — restore known-good Worker after a staging build reached production (admin-token gate 403d the live Hub). No functional change.
