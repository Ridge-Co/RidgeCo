{
  "sheet_id": "1KggRJBeJg6WDElisEQmAEsmB0hXtoNBIYWbOMFCd4S4",
  "description": "Aug 11 2026 (retry) — add optional 'Room' column to Work_Orders. The prior combined op appended the Bedroom-3 key (ID 68, verified present in Keys) but did not clear pending.json, so the column add did not complete. This op is column-only: the key append is intentionally omitted to avoid a duplicate key row. add_column_header is idempotent (no-op if 'Room' already exists).",
  "ops": [
    { "type": "add_column_header", "tab": "Work_Orders", "header": "Room" }
  ]
}
