# MOVED — CREDENTIALS_MAP.md is now in `brett332/data`

**This file moved Sep 16, 2026.** It named real service-account emails, a Google Sheet ID, and a
Worker URL — never secret values, but identifiers with no reason to sit in a *public* repo when a
private one is available for exactly this purpose.

**Read it at:** `brett332/data/CREDENTIALS_MAP.md` — via GH Broker: `read_file(repo="brett332/data", path="CREDENTIALS_MAP.md")`.

This stub stays here permanently so any session, doc, or habit still pointing at
`context/CREDENTIALS_MAP.md` in this repo lands on a clear redirect instead of a 404.

Nothing else about credentials access changed — GH Broker is still the primary path for all
GitHub read/write, PAT is still fallback-only. See `CLAUDE.md` for the current instructions.
